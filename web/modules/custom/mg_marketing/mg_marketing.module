<?php

/**
 * @file
 * Primary module hooks for Mg marketing.
 */

/**
 * Implements hook_form_alter().
 */
function mg_marketing_form_alter(&$form, &$form_state, $form_id) {
//bad comment.
  // \Drupal::request()->getUri()
  if($form_id =='search_form') {
    $form['basic']['submit']['#value'] = t('Search now!!!');
  }

};
